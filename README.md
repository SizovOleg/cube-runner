# Cube Runner: Battle Dash 🎮

2D платформер/раннер с боссами на Canvas + React + TypeScript.

## Запуск

```bash
npm install
npm run dev
```

## Сборка и деплой

```bash
npm run build              # → dist/
./server/deploy.sh         # → сервер
```

## Структура

```
src/
├── engine/      # Движок: цикл, физика, камера, ввод, рендер
├── entities/    # Игрок, враги, боссы, пули, powerups
├── levels/      # Система уровней, данные уровней
├── ui/          # HUD, меню, экран паузы
└── utils/       # Константы, математика, localStorage
```

## Управление

| Действие | Клавиатура | Тач |
|----------|-----------|-----|
| Прыжок/Полёт | Space / W / ↑ | Тап по экрану |
| Стрельба | X / Z / Shift | Кнопка GUN |
| Powerup 1-3 | 1, 2, 3 | Кнопки слотов |
| Пауза | Esc / P | — |

## Лицензия

MIT
